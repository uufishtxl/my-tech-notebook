# 待复习错题

> 下次测验时，这些错题会作为简答题优先出题。答对后打勾 `[x]` 移除。

## 后端
- [x] ACID 的 D 是什么？（提示：不是 Duration）
- [x] 什么是 N+1 查询问题？如何解决？（用 SQLAlchemy 的 `joinedload`/`selectinload`）
- [x] 在 DRF 中，如何添加 Model 上不存在的动态字段？（SerializerMethodField）
- [x] StreamingHttpResponse 和普通 HttpResponse 在内存处理上的核心区别？
- [x] 在 Python 中，`*args` 和 `**kwargs` 分别被打包成什么数据结构？
- [x] 什么是浅拷贝 (Shallow Copy)？修改嵌套列表时会发生什么？
- [x] 带有 `yield` 的函数在调用时会返回什么对象？
- [x] TypeScript 的 `Partial<T>` 有什么作用？
- [x] HTTP 的 Connection Timeout 和 Read Timeout 分别对应什么场景？
- [ ] SQLAlchemy 中对应 select_related 和 prefetch_related 的方法名（joinedload / selectinload）
- [ ] Pathlib 中 `path.parts` 的作用及其在文件夹过滤中的优势
- [ ] 彻底区分 Throttle (节流) 和 Debounce (防抖) 的例子
- [ ] `<note type="warning">` 和 `<note type="caution">` 有什么区别？
- [ ] `keyref` 和 `conref` 的区别是什么？

## 前端
- [ ] `throttle` 和 `debounce` 的区别是什么？各适用于什么场景？

## DITA
- [ ] `<note type="warning">` 和 `<note type="caution">` 有什么区别？
- [ ] `keyref` 和 `conref` 的区别是什么？

## DITA
(暂无待复习项 🎉)
