## lingua-workbench

* [x] 开启 review 时，如果还有 Pending Translation 的部分，跳转到 Translation。
* [x] Translation 完成后，包含可以直接跳转到 `/review` 的页面
* [ ] 查看 AI 客服相关代码
	* [x] 完成 dita_parser.py
	* [x] 完成 Command
	* [ ] 接着看 services 中的 build_index
* [x] 目前自动登录没用了

## 02-03

* [x] Vibe Coding 新功能实现
	* [x] 解析
	* [x] 翻译
	* [x] 前端显示
	* [x] 前端标记
	* [x] 推至下一个 Chunk
* [ ] 翻译模式Review
    两种模式：
	* [x] 按 Chunk ID 循环 Review 模式
	* [ ] 查看所有标黄/红的句子
* [x] 标记“今日复习到此处” （回形针）。得想一想，后端如何标记。因为可能需要有一个入口，可以直接跳转至上次复习到的位置。因此，这是一个唯一的值，也就是说每次更新这个值，上一个值就会无效。
   或者，在末尾设置一个按钮：【完成并继续】。如果是完成并继续，那么更新这个chunk的最后完成时间，并跳转至下一个chunk。这样进度就可以从最近一个日期最后完成的chunk_id 开始算。
	关于【完成并继续】，是否可以设置一个上级 Component 的变量，并向下inject至子组件。如果是完成并继续的，就会将 inChunkReviewMode.value 设置为 true，那么如果是在这种情况进入下一个chunk的，那么默认 Side Panel 就是打开的，并且整个最上方的 BaseWaveSurfer 的实例的 loop 设为 false。
* [ ] AudioBench 本身需要有一个跳转到下一个的按钮。
* [ ] Review 页面输入时，不能按空格，不能按回车，不能按方向键，都会触发事件。
* [ ] 考虑下dashboard页面：
	* [ ] 如果最近一次进度在10/13，应该查询下一集，或下一季；如果没有，Resume Learning可能是：Start Fresh
	* [ ] dashboard 页面在笔记本屏幕上会需要滚动，需要修改。
* [ ] 目前脚本获取和翻译的 API 都还停留在后端层面。可以考虑，当创建新的 AudioSource 时，会在post-save 自动执行这两步操作拉取脚本。如果没有拉取到，需要给用户一个系统通知。因此，可能需要一个系统通知的简单界面。

![[Pasted image 20260203220559.png]]
---

![[Pasted image 20260203220611.png]]
### 🎯 开发目标：实现“剧本流伴随与动态切分”功能 (Script Side Panel & Relay Workflow)

**项目背景**：在现有的 Audio Slicer Workbench 中，新增一个右侧面板，用于展示从网页抓取的整集剧本。用户通过“接力棒”模式（Relay Mode），在学习过程中手动将剧本切分给后续的 Audio Chunk。


# Feature Spec: Script Side Panel & Workflow

## 1. 核心概念 (Core Concept)

一个伴随音频切片学习的 **“流式剧本阅读器”**。

- **初始状态**：整集剧本全部挂载在第 1 个 Chunk 下。
    
- **交互模式**：用户在学习过程中，手动“剪切”剧本，将剩余部分“推”给下一个 Chunk（接力棒模式）。
    
- **数据结构**：结构化存储，分离“对白”与“动作/场景”，支持复习高亮和音频弱关联。
    

---

## 2. 数据库设计 (Data Model)

文件: `scripts/models.py`

```Python
class ScriptLine(models.Model):
    # --- 1. 定位与顺序 ---
    # 归属的大任务/Chunk。初始全部指向该集第一个Chunk，随用户操作更新。
    chunk_id = models.ForeignKey('AudioChunk', on_delete=models.CASCADE, related_name='script_lines')
    # 绝对顺序 (0, 1, 2...)，保证渲染顺序
    index = models.IntegerField(db_index=True)

    # --- 2. 类型与内容 ---
    TYPE_CHOICES = [
        ('dialogue', '对话'),      # 有人说话
        ('action',   '独立动作'),  # 没人说话的纯动作行
        ('scene',    '场景标题'),  # [Scene: ...]
    ]
    line_type = models.CharField(max_length=20, choices=TYPE_CHOICES, default='dialogue')

    speaker = models.CharField(max_length=100, null=True, blank=True)
    
    # 核心展示文本 (Clean Text)。
    # dialogue: "Watch me!" (不含动作)
    # action/scene: 完整内容
    text = models.TextField()

    # 附属动作 (Metadata)。
    # 仅当 dialogue 中穿插动作时使用，如 "he drinks milk"
    action_note = models.TextField(null=True, blank=True)

    # 原始文本 (Backup)
    raw_text = models.TextField()
    zh_text = models.TextField(null=True, blank=True)

    # --- 3. 关联与状态 ---
    # 弱关联：具体的 AudioSlice ID (点击查找后绑定)
    slice_id = models.ForeignKey('AudioSlice', null=True, blank=True, on_delete=models.SET_NULL)
    
    # 复习高亮
    HIGHLIGHT_CHOICES = [('none', 'None'), ('yellow', 'Review'), ('red', 'Hard')]
    hili_type = models.CharField(max_length=20, choices=HIGHLIGHT_CHOICES, default='none')

    class Meta:
        ordering = ['chunk_id', 'index']
        indexes = [models.Index(fields=['chunk_id', 'index'])]
```

---

## 3. 后端逻辑 (Backend Logic)

### A. 解析与入库 (Ingestion)

- **目标**：从网页抓取，清洗数据，全部存入 **Target Chunk ID** (该集第1个Chunk)。
    
- **清洗策略**：
    
    - **Scene**: 识别 `[Scene: ...]` -> `type='scene'`
        
    - **Dialogue**: 识别 `Joey: ...`
        
        - 将 `(...)` 内容提取到 `action_note`。
            
        - 剔除括号后的纯文本存入 `text`。
            
    - **Standalone Action**: 无法匹配 Speaker 且包含括号 -> `type='action'`。
    - 中间 `<h3>` tag，并且不包含 [] (Scene) 或者 () （旁白或动作）可以忽略
        
示例剧本：

```Text
### [Scene: Central Perk. Everyone’s sitting on the couch and Phoebe enters.]

**Phoebe**: Hi

**All**: Hey! Hi!

**Rachel**: How was the honeymoon?

### **OPENING CREDITS**

### [Scene: Central Perk. Phoebe’s trying to teach Joey French, so she’s sitting in front of him with the script in her hands.]

**Phoebe**: All right, it seems pretty simple. Your first line is "My name is Claude", so, just repeat after me. "Je m’appelle Claude".

**Joey**: Je de coup Clow.

(Joey takes the plastic container to his mouth and starts to drink. Most of the milk gushes from the bottle down his chin and over his clothes to the floor. He keeps "drinking" and all of a sudden he lifts it up and half the bottle of milk pours out in an instant. He then continues to drink the rest. He then puts the empty container down on the counter.)

**Phoebe**: (checking her watch) You did it !

### End

**_© [Fan Club Français de Friends](http://www.fanfr.com/) & [Friends Generation 2](http://www.friendsgeneration.com/)_**
```
### B. 读取接口 (Fetch API)

- **逻辑**：不需要加载全量数据。
    
- **优化**：`GET /workbench/:id/script` 默认只返回 `index` 最小的前 50 行**。
    
    - _理由_：用户只会操作前几十行来切分，后面的即使加载了也会被切走。
        

### C. 剪切接口 (Split API) - 核心功能

- **Endpoint**: `POST /workbench/:id/split`
    
- **Payload**: `{ "start_index": 21, "next_chunk_id": 14 }`
    
- **SQL 逻辑**:

    
    ```SQL
    UPDATE script_line 
    SET chunk_id = 14 
    WHERE chunk_id = 13 AND index >= 21;
    ```
    
- **Undo 逻辑 (后悔药)**:
    
    - 前端保留本次操作的 Context。
        
    - 如果 Undo，调用反向接口把 chunk_id 改回 13。
        

---

## 4. 前端 UI/UX (Side Panel)

### A. 布局与渲染

- **容器**：右侧独立 Side Panel，单列流式布局。
    
- **渲染样式**：
    
    - **Dialogue**: 气泡样式。`action_note` 显示在气泡下方/旁边的灰色小字 (Subtext)。
        
    - **Action**: 居中、灰色、斜体、无气泡。
        
    - **Scene**: 分割线样式 `—— Central Perk ——`。
        

### B. 交互设计：剪切 (The Split)

- **触发入口**：
    
    - **默认隐形**：鼠标 Hover 到某一行时显示。
        
    - **图标位置**：行最左侧 (Left Sidebar of the row)。
        
    - **图标样式**：`->|` (Next / Push Icon)。
        
    - **Tooltip**: _"Start [Chunk #14] from here"_ (动态显示下一个 Chunk 的名字/ID)。
        
- **点击反馈**：
    
    - **动画**：被切分出去的行（当前行及之后所有）执行 **Slide Down + Fade Out** (向下划出并消失)。
        
    - **数据**：列表瞬间变短，只剩切分点之上的内容。
        
- **后悔药 (Toast)**：
    
    - 底部弹出 Toast: _"Moved 450 lines to Chunk #14. [Undo]"_。
        
    - 点击 Undo 立即恢复。
        

### C. 交互设计：查找与定位 (Search & Anchor)

- **触发**：Hover 行右侧显示 `🔍`。
    
- **逻辑**：
    
    - 点击 `🔍` -> 在左侧 Audio Slices 中搜索匹配文本。
        
    - **命中**：左侧滚动到对应卡片 + 卡片闪烁高亮 + 自动保存 `slice_id` 到该行。
        
    - **未命中**：Toast 提示 "No audio found"。
        
- **已连接状态**：如果该行已有 `slice_id`，显示微弱的绿色信号图标 `((·))` 或文字加深。
    

---

## 5. 开发路线图 (Dev Checklist)

1. **Model Migration**: 创建/更新 `ScriptLine` 表结构。
    
2. **Parser Script**: 编写 Python 脚本，跑通 `fanfr.com` 的抓取，将 S10E13 存入 Chunk #13 (假设)。
    
3. **API Dev**: 写好 `list` (limit 100) 和 `split` (update) 两个接口。
    
4. **Frontend List**: 渲染右侧基础列表 (区分三种类型样式)。
    
5. **Frontend Split**: 实现 Hover 图标、点击 API 调用、以及最重要的 **消失动画** 和 **Undo Toast**。
    
6. **Integration**: 联调，享受丝滑的“推入”体验。



总结下功能：

### Load 

- **Loading 态**：Load 页面时，对旧内容应用高斯模糊 (Blur)，并在中间显示 Loading Spinner。
- **GET 请求**：带 Pagination 的请求，核心参数：
    - `mode`: 'shuffle' | 'normal' (对应 Shuffle 模式)
    - `status`: 'review' | 'hard' | 'all' (对应难度筛选)
    - `character`: 'All' | 'Chandler' | ... (对应角色筛选)
    - `page`: 页码
    点击头像后，Shuffle 仍然保持当前的状态。
- **滚动加载**：根据配置中的 limit 数量（6的倍数，>= 12），到达底部时触发 Infinite Scroll 加载下一页数据。

### Top Bar

- **难度筛选**：点击 `color` 图标切换。
	- 逻辑：显示/隐藏 Review 或 Hard 项，**至少选中一项**（不能全空）。
	- UI：选中显示填充色 (Fill)，未选中显示描边 (Stroke)。
	- 动作：触发 API 重新请求。
- **图标 1 (Shuffle)**：
    - 点击切换 Shuffle 模式状态。
    - 状态：开启显示彩色，关闭显示灰色。
    - 动作：带上 `mode=shuffle/normal` 重新提交 Request，重置 page=1，并弹出 Toast 提示 "Shuffle On/Off"。
- **图标 2 (Settings)**：(暂不做功能，预留位置)
    - _未来规划：倍速、PageSize、自动播放开关、快捷键映射等。_

### Avatar Collection
- **交互逻辑**：
    - 点击 All：显示所有角色。
    - 点击特定头像：发送 API 请求（例：`GET /api/blitz/cards?character=Rachel&page=1&mode=current_mode`）。
    - **前端动作**：设置 `selectedCharacter` -> **重置 `page = 1`** -> 清空列表 -> Loading -> 请求数据。
- **动画效果 (Dock Effect)**：
    - **Hover Target**：放大至 120% (`scale-125`)，透明度 1，层级最高 (`z-10`)，并轻微上浮。
    - **Siblings (其他头像)**：透明度降至 0.2，并缩小 (`scale-90`)。
    - **Displacement (分海效果)**：Hover 头像左侧的所有头像向左平移 (`-translate-x`)，右侧的向右平移，产生物理挤压感。

### Line Cards Layout

- **布局**：Flex-row wrap + 负边距 (`-m`) 处理间距。
- **响应式逻辑** (最小宽 360px)：
    - **Mobile (< 768px)**: 1 列 (`w-full`)
    - **Tablet (>= 768px)**: 2 列 (`w-1/2`)
    - **Laptop (>= 1150px)**: 3 列 (`w-1/3`)
    - **Desktop (>= 1536px)**: 4 列 (`w-1/4`)

### Line Card（单卡片交互）

- **Flip Logic**：
    - 默认显示正面 (Light Theme)。
    - **触发**：仅 **点击头像** 或 **点击顶部彩色区域** 时翻面（避免误触底部按钮）。
    - 翻转后默认 **不** 自动播放音频。
- **Front (正面)**：
    - 显示：剧集来源 (S10E12) + 中文译文。
- **Back (反面 - Dark Theme)**：
    - 显示：英文原文 + 底部 Controllers。
    - **底部 Controllers (Flex 布局)**：
        - **左侧 (Status)**：标记图标。
            - Review (#FADA3E) / Hard (#F24822)。    
            - 点击取消标记（API请求）-> 变为 **Undo** 图标 -> 点击恢复标记。
        - **中间 (Record Hub)**：
            - 使用 `useRecording` composable。
            - **初始状态**：仅显示【麦克风】图标。
            - **录音完成动画**：【麦克风】图标保持在右侧（或向右微移），**【播放自己】图标在左侧淡入并弹出** (Scale 1.2 -> 1.0)。形成 `[Play Self] [Record]` 的布局。
        - **右侧 (Original Audio)**：
            - 使用 `useAudio` composable。
            - 【播放原音】图标 (彩色)。播放时变【暂停】，结束后变回【播放】。
- **文本**：根据字数动态调整字号 (Text Scaling)，防止长句溢出。

### 账单

### 2/5
- 游戏 6
- 阿托品 298
- 挂号 6
- 张亮 17
### 2/4
- 9.4 面条+榨菜
- 240 燃气费
### 2/3
- 94.94 叮咚买菜（全部是买菜）
- 13.92 零食很忙
- 75.26 盒马（牛奶51，蜜瓜 11，其他虾滑）
### 2/2
- 54.65 叮咚买菜
	- 水果：22
	- 其他蔬菜
- 17.94 樱桃蛋糕
- 60
	- 粑粑柑 36.9
	- 吐司 7.5
	- WSJ 15.6
### 2/1
- 红枣 9.9
- 糖藕 8.72
- 香蕉 3.99
- Labubu 塑料壳 3.28
- 叮咚买菜 92.09（全部买菜）
- 苹果 42.91
- 电费父母家 104.2
- 电费我家 152.3
- 移动 1.99